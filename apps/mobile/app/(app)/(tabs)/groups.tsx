/** /groups route: renders GroupsScreen. */

import { GroupsScreen } from "@/screens/groups/components/GroupsScreen/GroupsScreen";

/**
 * Renders the groups route by mounting the GroupsScreen orchestrator.
 *
 * @returns The /groups screen.
 */
export default function Groups() {
  return <GroupsScreen />;
}
