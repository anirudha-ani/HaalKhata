/** /account route: renders AccountScreen. */

import { AccountScreen } from "@/screens/account/components/AccountScreen/AccountScreen";

/**
 * Renders the account route by mounting the AccountScreen orchestrator.
 *
 * @returns The /account screen.
 */
export default function Account() {
  return <AccountScreen />;
}
