/** /activity route: renders ActivityScreen. */

import { ActivityScreen } from "@/screens/activity/components/ActivityScreen/ActivityScreen";

/**
 * Renders the activity route by mounting the ActivityScreen orchestrator.
 *
 * @returns The /activity screen.
 */
export default function Activity() {
  return <ActivityScreen />;
}
