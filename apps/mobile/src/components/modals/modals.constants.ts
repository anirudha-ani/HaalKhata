/** Constants for the modal sheets. */

/**
 * How long the "Copied" confirmation stays on the copy button, in
 * milliseconds. Long enough to read, short enough that a second copy is
 * never waiting on it.
 */
export const COPIED_BADGE_MS = 1500;
