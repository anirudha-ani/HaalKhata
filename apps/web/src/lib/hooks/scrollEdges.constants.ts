/** Constants for the scroll-edge hook. */

/**
 * Slack, in pixels, when deciding whether a scroll container is at an edge.
 * Fractional scroll positions and sub-pixel sizes mean "at the end" is rarely
 * an exact equality.
 */
export const SCROLL_EDGE_TOLERANCE_PX = 1;
