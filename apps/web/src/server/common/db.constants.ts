/** Database constants. */

/** Dev-default connection string, matching the compose `db` service; DATABASE_URL overrides it. */
export const DEFAULT_DATABASE_URL = "postgres://haalkhata:haalkhata@localhost:5432/haalkhata";
